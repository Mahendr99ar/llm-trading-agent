# Core dependencies
yfinance>=0.2.28
feedparser>=6.0.10
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0

# Optional: FinBERT sentiment (better accuracy than rule-based)
# Install only if you want use_finbert=True
# pip install transformers torch
# transformers>=4.30.0
# torch>=2.0.0
